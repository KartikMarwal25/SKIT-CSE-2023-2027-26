/**
 * Scaffold placeholder — the real routing/pages land once the wireframe
 * review (docs/design/wireframe-review-notes.md) has settled the three
 * portal flows this app needs.
 */
export function App() {
  return (
    <main className="flex min-h-screen items-center justify-center bg-paper">
      <h1 className="text-2xl font-bold text-ink">SecureCred</h1>
    </main>
  );
}
